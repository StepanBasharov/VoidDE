# 🔥 Kali Nvim - Хакерские фичи

## 🎨 Визуальные улучшения

### Matrix-style тема
- ✅ Прозрачный фон (видно терминал)
- ✅ Зелёные акценты (#00ff41) как в Matrix
- ✅ Тёмная storm тема
- ✅ Кастомный Kali ASCII art при запуске

## 🛠️ Инструменты пентестера

### Hex Viewer
```vim
:HexDump     " Открыть файл в hex режиме
:HexToggle   " Переключить hex/text режим
```

### Base64 Encoding/Decoding
| Режим | Клавиши | Описание |
|-------|---------|----------|
| Visual | `<leader>be` | Base64 Encode выделенного |
| Visual | `<leader>bd` | Base64 Decode выделенного |

### Hash Calculator
| Клавиши | Описание |
|---------|----------|
| `<leader>hm` | MD5 hash (копируется в clipboard) |
| `<leader>hs` | SHA256 hash (копируется в clipboard) |

### Quick Script Execution
| Клавиши | Описание |
|---------|----------|
| `<leader>xe` | chmod +x текущий файл |
| `<leader>xp` | Запустить Python скрипт в split |
| `<leader>xb` | Запустить Bash скрипт в split |

## 💻 Поддержка языков

### Полностью поддерживается:
- ✅ Python (pyright + ruff)
- ✅ Go (gopls)
- ✅ C/C++ (добавь clangd)
- ✅ Assembly (nasm/asm)
- ✅ Bash/Shell
- ✅ Ruby (для Metasploit)
- ✅ Perl
- ✅ PHP
- ✅ Lua (для Nmap scripts)
- ✅ SQL

## 🎯 Workflow для пентеста

### 1. Написание эксплойта
```vim
:e exploit.py              " Создать файл
<leader>xe                 " Сделать исполняемым
<leader>xp                 " Запустить в split
```

### 2. Работа с payload
```vim
# Выдели payload в Visual mode
<leader>be                 " Закодировать в base64
<leader>hs                 " Получить SHA256 hash
```

### 3. Анализ бинарников
```vim
:HexDump binary.bin        " Открыть в hex режиме
```

### 4. Быстрый поиск в коде
```vim
<leader>fg                 " Live grep по проекту
<leader>fd                 " Найти diagnostics/ошибки
```

## 🚀 Dashboard Commands

При запуске Neovim:
- `f` - Find File
- `n` - New Script
- `g` - Search Code
- `r` - Recent Files
- `c` - Config
- `l` - Lazy (plugins)
- `m` - Mason (LSP tools)
- `q` - Quit

## ⚡ Pro Tips

1. **Прозрачность**: Работает лучше всего с терминалами с прозрачностью
2. **Hex mode**: Отлично для анализа бинарников и shellcode
3. **Split execution**: Запускай скрипты без выхода из редактора
4. **Hash в clipboard**: После `<leader>hm` hash уже в буфере обмена

## 🔧 Добавить ещё:

Хочешь добавить поддержку нового языка? Например, для C:
```lua
-- lua/plugins/lsp.lua
vim.lsp.config("clangd", {
    cmd = { "clangd" },
    filetypes = { "c", "cpp" },
    ...
})
```

---

**"The quieter you become, the more you are able to hear"** 🐉

